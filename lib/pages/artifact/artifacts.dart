import 'package:flutter/material.dart';

class MyArtifacts extends StatelessWidget {
  const MyArtifacts({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Artifact'),
    );
  }
}
